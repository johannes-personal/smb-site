# What the template actually contains

<!-- GENERATED by scripts/manifest.py — do not edit by hand. Regenerate after
     changing assets/template/. -->

**This file is generated from `assets/template/` on disk.** Everything listed
here exists. Anything not listed here does not exist yet, whatever the other
reference files may say about it in the present tense.

That distinction is the whole point of this document. `03-architecture.md`
described a magic-link login in the present indicative; there was no login
page, no callback route, and nothing that wrote a session cookie. The prose was
an intention and was read as an inventory. Read the prose for what to *decide*
and *build*; read this for what you *have*.

**Engine** (`@smb-site/engine`): 29 files, 2433 lines — a
dependency, shared by every project.
**Shell** (`assets/template/`): 45 files, 2348 lines — copied once per
project, then owned by it.

Verified by
`python scripts/selftest.py`, which scaffolds it and runs typecheck, tests and
a production build — see `.selftest.json` for when that last passed.

## Routes

| Path | What it is |
|---|---|
| `/[[...slug]]` | page — SSR-capable by default even where output is effectively static. |
| `/beheer/[collection]` | page — The third owner surface, alongside "Mijn gegevens" and "Pagina's". |
| `/beheer` | layout — Everything under /beheer requires a signed-in user. |
| `/beheer` | page — "Mijn gegevens" — the surface the owner actually uses. |
| `/beheer/paginas/[slug]` | page — "Pagina's" — the canvas. |
| `/beheer/taxonomie` | page — The lists behind the product form's dropdowns. |
| `/inloggen` | page |
| `/` | layout — The root layout reads facts, so every route under it — including Next's built-in /_not-found — needs |
| `/api/publish` | API route — Writes go through here so the service-role key never reaches the browser. |
| `/api/seed` | API route — A few thousand rows in batches of 500 takes several seconds; the default 10s is tight for any projec |
| `/api/taxonomie` | API route — Saves the three taxonomy lists. |
| `/auth/callback` | API route — Where the magic link lands. |

## Blocks in the Puck registry

| Component | Label the owner sees |
|---|---|
| `Hero` | Openingsblok |
| `PageIntro` | Paginakop |
| `QuickLinks` | Snelkoppelingen |
| `FeatureRow` | Uitgelicht |
| `CardGrid` | Kaarten |
| `PriceTable` | Prijzen |
| `HoursSummary` | Openingstijden (kort) |
| `HoursCalendar` | Openingstijden (kalender) |
| `Gallery` | Fotogalerij |
| `TextSection` | Tekst |
| `FAQ` | Veelgestelde vragen |
| `CTABand` | Oproepbalk |
| `MapContact` | Adres en route |
| `Embed` | Extern blok (tickets, reserveren, video) |
| `Testimonials` | Reacties van klanten |
| `ProductGrid` | Uit de collectie |

## Engine modules — import from @smb-site/engine

| Module | Exports |
|---|---|
| `collections.ts` | COLLECTIONS, ClientCollectionDef, CollectionDef, CollectionItem, FieldDef, OwnerItem, PRODUCTEN, formatEuro, labelsFrom, resolveItem, setTaxonomyOptions, spreadByBrand, toClientDef, toOwnerItems |
| `facts.ts` | BrandItem, Facts, HoursFact, HoursRule, NapFact, NavItem, Predicate, PriceTier, PricesFact, Season, ServiceItem, TaxonomyItem, formatPrice, pickTiers |
| `hours.ts` | DayStatus, WeekDay, describeNextOpen, isOpenNow, monthStatuses, nextOpen, statusForDate, weekSchedule |
| `jsonld.ts` | localBusinessJsonLd |
| `links.ts` | ExternalProps, externalProps, isExternal |
| `puck.config.tsx` | Components, buildConfig |
| `taxonomy.ts` | FIELD_FOR, KIND_LABEL, TaxonomyKind, TaxonomyProblem, brands, categories, departments, itemsOf, labelFor, labelsOf, optionsOf, slugify, taxonomyProblems, uniqueSlug, usageCounts |
| `types.ts` | BlockProps, ResolvedItem |

## Shell modules — the project owns these

| Module | Exports |
|---|---|
| `lib/collections.ts` | ItemQuery, countsBy, getCatalogue, getItem, getItems |
| `lib/facts.ts` | clearFactsCache, getFacts |
| `lib/supabase-browser.ts` | createClient |
| `lib/supabase-server.ts` | createSessionClient, getUser |
| `lib/supabase.ts` | createAdminClient, createServerClient |

## Keys on the Facts type

| Key | Required? |
|---|---|
| `hours` | required |
| `prices` | optional |
| `nap` | required |
| `services` | required |
| `brands` | optional |
| `categories` | optional |
| `departments` | optional |
| `socials` | required |
| `meta` | required |
| `_provenance` | optional |

## Tests that ship with the template

| File | What it holds the line on |
|---|---|
| `engine/tests/hours.test.ts` | The opening hours are the most consequential thing on a shop's website — more visits are lost to "is |
| `engine/tests/links.test.ts` | These hand off to a dialer or a mail client. |
| `engine/tests/taxonomy.test.ts` | The taxonomy used to be two `as const` arrays in two files: the product form read one, the URL route |
| `tests/boundary.test.ts` | The server/client boundary is invisible to TypeScript and to `next build`. |
| `tests/content.test.ts` | Content is data, so it can be wrong in ways TypeScript cannot see: a price off by a factor of a hund |

## What the template does NOT include

Generated by absence, so treat it as a prompt rather than a guarantee — check
the lists above before assuming something is missing.

- **A collection.** `lib/collections.ts` ships a `PRODUCTEN` definition as a
  worked example. A project without a catalogue should delete it; a project
  with one should replace it. See `07-collections.md`.
- **Content.** `content/facts.json` and `content/pages.json` ship as
  placeholders. Phase 4 replaces them.
- **A favicon of the business's own.** `app/icon.svg` is a placeholder circle.
- **Redirects.** `next.config.ts` ships empty; Phase 3's `redirects.csv` is the
  input.
- **Anything sector-specific.** The sector packs are guidance, not code.
